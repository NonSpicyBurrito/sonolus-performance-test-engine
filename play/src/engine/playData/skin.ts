import { SkinSpriteName } from '@sonolus/core'

export const skin = defineSkin({
    sprites: {
        parallel: SkinSpriteName.NoteHeadRed,
        sequential: SkinSpriteName.NoteHeadGreen,
    },
})
